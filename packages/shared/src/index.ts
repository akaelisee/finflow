/**
 * @finflow/shared — types et schémas partagés entre le front et le back.
 *
 * Ce package sera utilisé par :
 *  - apps/api (validation des requêtes entrantes)
 *  - apps/web (validation des formulaires et typage des réponses API)
 *
 * L'énorme avantage du monorepo : si on change un schéma ici,
 * le front et le back sont mis à jour ensemble.
 */

// Placeholder — on remplira ce fichier à l'étape 4 avec les schémas Zod
// (CreateAccountSchema, CreateTransactionSchema, etc.)

export const SHARED_PACKAGE_VERSION = '0.1.0';