export type Categories = {
    userId: string,
    name: string,
    color: string,
    icon: string,
    isDefault: boolean
}

export interface CategoriesPlayload {
    userId: string,
    name: string,
    color: string,
    icon: string,
    isDefault: boolean
}