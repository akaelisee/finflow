export type Categories = {
    name: string,
    color: string,
    icon: string,
    isDefault: boolean
}

export interface CategoriesPlayload {
    name: string,
    color: string,
    icon: string,
    isDefault: boolean
}